services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: paltalabs
      POSTGRES_PASSWORD: paltalabs
      POSTGRES_DB: paltalabs
    ports:
      - '${DB_HOST_PORT:-5432}:5432'
    volumes:
      - dbdata:/var/lib/postgresql/data

volumes:
  dbdata:
